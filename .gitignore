__pycache__/
*.py[cod]
.venv/
venv/
.env
.env.local
chunks.json
.DS_Store
*.zip
docs/*.pdf
.prefix_cache.json

# Артефакты прогонов: воспроизводятся make eval / python -m rag.trace,
# в истории от них только шум и конфликты.
eval_*.txt
trace_*.txt
rerank_scores.csv
score_log.jsonl

# Выгруженные ONNX-модели реранкера: 2.8 ГБ, воспроизводятся
# scripts/export_reranker_onnx.sh. В git им делать нечего.
models/
