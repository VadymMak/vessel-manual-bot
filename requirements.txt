# ─── Этап 1: ingestion ───────────────────────────────────────────────────────
PyMuPDF>=1.24

# ─── Этап 2: RAG-ядро ────────────────────────────────────────────────────────
psycopg[binary]>=3.1          # драйвер PostgreSQL 16
pgvector>=0.3.0               # типы VECTOR / SPARSEVEC для psycopg3
FlagEmbedding>=1.2            # BGEM3FlagModel + FlagReranker

# ВЕТКА 4.x ОБЯЗАТЕЛЬНА, верхняя граница не косметическая.
# FlagEmbedding 1.4.0 написан под transformers 4.x. На transformers 5.x импорт
# падает внутри самого transformers:
#   transformers/integrations/tensor_parallel.py:465
#   NameError: name 'torch' is not defined
# Ветка 5 требует torch >= 2.4, а тут 2.2 — отсюда несвязанный импорт.
# Проверено на macOS, Python 3.11, transformers 5.14.1 + torch 2.2.2.
transformers>=4.40,<5

torch>=2.2                    # CPU backend; дискретной GPU нет

# numpy 1.x ОБЯЗАТЕЛЕН, и причина не в numpy.
# torch 2.2.2 собран против NumPy 1.x C API (_ARRAY_API). На numpy 2.x импорт
# torch выдаёт «Failed to initialize NumPy: _ARRAY_API not found» и torch
# остаётся полуживым.
# Обновить torch нельзя: 2.2.2 — последняя версия с колёсами под macOS x86_64,
# сборки под Intel-Mac прекращены в январе 2024.
# На Linux-сервере ограничение неактуально — там доступен torch 2.4+.
numpy<2
openai>=1.0                   # gpt-4o-mini: contextual prefix + генерация
pydantic-settings>=2.0        # rag/config.py
click>=8.1                    # rag/cli.py, eval/run.py
pyyaml>=6.0                   # eval/golden_set.yaml

# ─── Этап 3+: FastAPI (пока не нужно) ────────────────────────────────────────
# fastapi>=0.111
# uvicorn[standard]>=0.29
